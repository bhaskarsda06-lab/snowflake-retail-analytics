CREATE OR REPLACE TABLE RAW.CUSTOMER_JSON
(
   DATA VARIANT
);

INSERT INTO RAW.CUSTOMER_JSON
SELECT PARSE_JSON(
'{
 "customer_id":1,
 "name":"John",
 "city":"New York"
}'
);

SELECT
DATA:customer_id,
DATA:name,
DATA:city
FROM RAW.CUSTOMER_JSON;
